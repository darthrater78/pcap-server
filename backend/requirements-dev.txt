-r requirements.txt
# 8.3.4 carried PYSEC-2026-1845. Dev-only -- the Dockerfile installs
# requirements.txt, so pytest was never in the shipped image -- but a scanner
# flagging the repo cannot tell that, and an advisory nobody can close is an
# advisory everybody learns to scroll past.
#
# pytest 9 drags pytest-asyncio with it: 0.25.2 pins pytest<9, so the two move
# together or not at all. Both suites were run against the pair before this was
# committed -- 929 API and 134 browser tests, no failures and no new warnings.
# asyncio_mode = "auto" and the function-scoped fixture loop in pyproject.toml
# are unchanged and still honoured.
pytest==9.1.1
pytest-asyncio==1.4.0
# Runs the suites across processes (scripts/check.sh passes -n). Each worker
# boots its own live server and chromium on its own port and temp dir, so the
# browser suites do not share state. -n 0 turns it off when debugging.
pytest-xdist==3.8.0
httpx==0.28.1
# starlette's TestClient prefers httpx2 (pydantic's successor to httpx) since
# starlette 1.7.0, and warns on every run when only httpx is present. httpx
# stays: the browser suites drive the live server with httpx.Client directly.
httpx2==2.13.1

# The browser suites (tests/browser) drive the real UI in a real chromium: the
# API suites cannot see a form with no way to submit it, or an inline script
# the CSP silently dropped. pip installs the driver, not the browser -- get the
# browser with `python -m playwright install chromium`, or point
# PCAP_TEST_CHROMIUM at one the environment already ships. Without it those
# tests report as SKIPPED, and scripts/check.sh says so before pytest runs.
playwright==1.63.0
