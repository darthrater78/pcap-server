fastapi==0.141.1
# Pinned explicitly rather than left to fastapi's transitive resolution, because
# the floor it allows carries known advisories. 0.50.0 was held here while
# fastapi 0.125.0 capped starlette at <0.51.0; that cap is gone (0.141.1 asks
# only for >=0.46.0), so this now moves to a release fixing five advisories that
# 0.50.0 carried:
#   PYSEC-2026-248 / -161  request.url rebuilt from an unvalidated path and an
#                          unvalidated Host header. This app reads
#                          request.url.path to decide which requests are
#                          refused over plain HTTP, so a path that reparses
#                          differently is a way past that control.
#   PYSEC-2026-249         request.form() silently ignoring max_fields and
#                          max_part_size for urlencoded bodies.
#   PYSEC-2026-2280        HTTPEndpoint dispatching on arbitrary attribute
#                          names (unused here).
#   PYSEC-2026-2281        StaticFiles UNC path traversal (Windows only; this
#                          ships as a Linux container).
# starlette 1.x drops on_event, so the shutdown handler is a lifespan
# context manager -- see _lifespan in main.py.
starlette==1.7.0
uvicorn[standard]==0.54.0
asyncssh==2.24.0
aiofiles==25.1.0
pydantic==2.13.5
python-multipart==0.0.32
pyotp==2.10.0
qrcode[pil]==8.2
cryptography==50.0.1
